{# {% set form = craft.formie.forms({ handle:'contactFormNL' ~ currentSite.handle|upper }).one() %} #}

<div class="container">
    <div class="row">
        <div class="col-full">
            {{ craft.formie.renderForm('contactFormNL') }}
        </div>
    </div>
</div>